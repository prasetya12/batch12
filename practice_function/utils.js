export function isGenap(number) {
    const numberBaru = number % 2 === 0
    return numberBaru
    // return numberBaru
    // return number % 2 === 0 //false
}

export function sapa(nama) {
    let content = `Hi,Saya ${nama}, semoga baik baik aja`
    return content
}